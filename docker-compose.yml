services:
  packet-storm:
    build: .
    image: packet-storm:latest
    container_name: packet-storm
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
